name: adr_in_app_review
description: A Flutter plugin for allowing users to submit review to playstore using google in app review api.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  plugin:
    platforms:
      android:
        package: me.muhrahmatullah.adr_in_app_review
        pluginClass: AdrInAppReviewPlugin

